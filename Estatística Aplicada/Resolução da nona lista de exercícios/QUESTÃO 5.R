prop.test(x = c(60, 40), n = c(400,225))

#O valor p do teste é 0.4263, que é maior que o nível de
#significância alfa = 0,05. Podemos concluir que a proporção de fumantes 
#é significativamente igual nos dois grupos com um valor de p = 0.4263.